{
  "name": "deployator",
  "version": "1.3.0-beta",
  "description": "Deploy releases over SSH with rsync, archive ZIP / TAR, symlinks, SCP",
  "main": "index.js",
  "bin": {
    "deployator": "dist/index.js"
  },
  "scripts": {
    "test": "mocha --compilers js:babel-core/register --require jsdom-global/register tests/**/*.spec.js",
    "build": "yarn babel src --out-dir dist"
  },
  "babel": {
    "presets": [
      ["@babel/env", {
        "targets": {
          "node": "8"
        }
      }
      ]
    ]
  },
  "author": "La Haute Société (http://www.lahautesociete.com)",
  "repository": "la-haute-societe/deployator",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.6",
    "@babel/preset-env": "^7.1.6",
    "chai": "^4.1.2",
    "jsdom": "^11.10.0",
    "jsdom-global": "^3.0.2",
    "mocha": "^4.1.0",
    "sinon": "^4.5.0",
    "tmp": "0.0.33"
  },
  "dependencies": {
    "extend": "^3.0.1",
    "ssh-deploy-release": "^2.3.0-beta",
    "yargs": "^10.0.3"
  }
}
